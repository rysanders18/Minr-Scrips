@namespace nfa

relative Vector3 pos01
relative Vector3 pos02
relative Vector3 norm01
relative Vector3 norm02
relative Boolean posMode = false

String[] room0-0listOfBranches = String[]
String[] room0-0listOfConnections = String[]

relative String[] listOfBranches = String[]
relative String[] listOfConnections = String[]

relative Long timeOfLastBranch
relative Int hotbarLevel = 0

# Functions

Double[] vecToXYZ(Vector3 v)
Vector3[] hermiteTwoPoint( Vector3 p0, Vector3 p1, Vector3 n0, Vector3 n1, Int steps, Double alpha)
nfaInteractBlock(Player player, Block block)
hotbarFunction(Player player)

String[] getEpsilonClosure(Player player, String blockCoords)
Boolean processString(Player player, String input)
visualizeStates(Player player, String[] states)
Boolean isAcceptState(String blockCoords)
String[] getStartStates(Player player)
String[] move(Player player, String[] states, String symbol)

Boolean runTest(String inputString, Player player, Boolean debug)

@endnamespace