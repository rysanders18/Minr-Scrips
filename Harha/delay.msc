# delay(Int seconds)

@using Harha
@fast

@define Long startTick = system::currentTimeMillis()
@define Long limit = Long(1000*seconds)

@for Int tickCount in list::range(0, seconds)
    @delay 1
    @if (system::currentTimeMillis() - startTick) >= limit
        @return
    @fi
@done